<%- include ("includes/header") %>
<div class="container">
    <div class="py-5 text-center">
        <h2>Orders</h2>
      </div>
    <div class="row">
<h3 class="display-5">
    Your Orders
</h3>
</div>
<hr class="mb-4">
<div class="row">
    <h3 class="display-5">
        <!-- <ul class="list-group mb-3">
            <% if(items.length > 0) { 
             for(let item of items{ %>
            <li class="list-group-item d-flex justify-content-between lh-condensed">
              <div>
                <h6 class="my-0">
                  <a href="/view/book/<%= item.bookId %>" alt="<%= value.name%>"
                    ><%= item.bookName %></a
                  >
                </h6>
              </div>
              <span class="text-muted" id="itemPrice">Rs <%= value.price %></span> 
              <button
                class="btn btn-xs btn-danger"
                data-id="<%= item.bookId %>"
              >
                <i class="fas fa-trash"></i>
              </button>
            </li>
            <% } %>
            <li class="list-group-item d-flex justify-content-between">
              <span>Total (INR)</span>
              <strong id="totalCost"><%= item.price  %></strong>
            </li>
            <% } %>
          </ul> -->
        </div>
    </h3>
    </div>
<hr class="mb-4">
<div class="row">
    <h3 class="display-5">
        Books you have borrowed
    </h3>
    </div>
    </div>
<%- include("includes/footer") %>
